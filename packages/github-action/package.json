{
  "name": "@freshstart-ci/github-action",
  "version": "0.1.0",
  "description": "GitHub Action wrapper for FreshstartCI.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsup",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@freshstart-ci/core": "workspace:*"
  }
}
